# Performance compilation

Compile instrument phrases into neutral notes, grouped attacks, articulations, controller curves
and pitch curves. Use a deterministic seed. Do not invent a library trigger; resolve it through a
profile and report fallbacks.


"""Project test package.

This marker keeps ``python -m unittest tests.<module>`` deterministic on Windows and
prevents an unrelated third-party package named ``tests`` from shadowing this directory.
"""
